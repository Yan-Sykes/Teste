# ============================================================================
# Streamlit Secrets Configuration Example
# ============================================================================
# Este arquivo mostra exemplos de configurações secretas que podem ser usadas
# Copie para secrets.toml e ajuste conforme necessário
# IMPORTANTE: secrets.toml não deve ser commitado no Git!
# ============================================================================

# Exemplo de configurações de performance
[performance]
max_cached_items = 100
cache_ttl_seconds = 300

# Exemplo de configurações de dados
[data]
max_rows_display = 1000
enable_lazy_loading = true
